machine:
  node:
    version: 4
test:
  override:
    - typings install
    - npm run build
    - npm run check
deployment:
  pre:
    - echo -e "$NPM_USER\n$NPM_PASS\n$NPM_EMAIL" | npm login
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    owner: d6u
    commands:
      - typings install
      - npm run build
      - npm publish
